# AI Skills Security Scanner

This repository contains a GitHub Action that:

- Scans markdown files in skills/
- Uses AI to detect malicious content
- Prints suspicious lines
- Fails workflow if malicious content is found

Triggers:
- Push to main
- Pull Request